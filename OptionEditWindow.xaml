<Window x:Class="CommandWizard.OptionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Edit Option" Height="260" Width="420" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Flag" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Flag, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Long Flag" VerticalAlignment="Center" Margin="0,10,0,0" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LongFlag, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Description" VerticalAlignment="Top" Margin="0,10,0,0" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,10,0,0" Height="80" AcceptsReturn="True" TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="90" Margin="0,0,8,0" Content="Cancel" Click="OnCancelClicked" />
            <Button Width="90" Content="Save" Style="{StaticResource PrimaryButtonStyle}" Click="OnSaveClicked" />
        </StackPanel>
    </Grid>
</Window>
